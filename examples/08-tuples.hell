main = do
  let demo :: (Int,Text) -> Text = \((x, y) :: (Int, Text)) -> y
  let foobar :: (Int, Text) = (123, "foo")
  Text.putStrLn (demo foobar)

  let (foo,bar) :: (Int, Text) = (123, "foo")
  Text.putStrLn bar
